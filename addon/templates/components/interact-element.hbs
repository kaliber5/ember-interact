{{#let
  (component
    "interact-base"
    style=style
    draggable=draggable
    resizable=resizable
    restrictToElement=restrictToElement
    onDragMove=(action "onMove")
    onResizeMove=(action "onResize")
    onDragEnd=(action "onInteractEnd")
    onResizeEnd=(action "onInteractEnd")

  ) as |base|}}
  {{#if (has-block-params)}}
    {{yield
      (hash
        base=base
      )
    }}
  {{else}}
    {{#component base}}
      {{yield}}
    {{/component}}
  {{/if}}
{{/let}}
